{
  "name": "testSW",
  "version": "0.0.1",
  "dependencies": {
    "ajv": "^6.10.2",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.0",
    "mocha-simple-html-reporter": "^1.1.0",
    "mochawesome": "^4.0.1",
    "should": "^13.2.3"
  },
  "scripts": {
    "test": "node_modules/.bin/_mocha --timeout 10000",
    "test1": "node_modules/.bin/_mocha --timeout 10000 --reporter mochawesome",
    "test3": "node_modules/.bin/_mocha --timeout 10000 --reporter mochawesome --reporter-options reportDir=customReportDir,reportFilename=customReportFilename",
    "test2": "node_modules/.bin/_mocha --timeout 10000 --reporter mocha-simple-html-reporter"
  }
}
